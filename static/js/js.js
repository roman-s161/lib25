// const buttons = document.querySelectorAll(".myButton");

// function random(number) {
//   return Math.floor(Math.random() * (number + 1));
// }

// for (let i = 0; i < buttons.length; i++) {
//   buttons[i].addEventListener("mouseover", function () {
//     const rgbCol =
//       "rgb(" + random(255) + "," + random(255) + "," + random(255) + ")";
//     buttons[i].style.backgroundColor = rgbCol;
//     buttons[i].style.color = "white";
//     buttons[i].style.transform = "scale(1.5)"; // Увеличить размер кнопки в 1.5 раза при наведении
//   });

//   buttons[i].addEventListener("mouseout", function () {
//     buttons[i].style.backgroundColor = "";
//     buttons[i].style.color = "";
//     buttons[i].style.transform = "scale(1)"; // Вернуть исходный размер кнопки при уходе курсора
//   });
// }


